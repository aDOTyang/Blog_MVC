@model IEnumerable<Blog_MVC.Models.BlogPost>
@using Blog_MVC.Enums
@using Blog_MVC.Services.Interfaces
@inject IImageService _ImageService

@{
}

@*
<div class="col-lg-8 col-xl-8">
    @foreach (BlogPost post in Model)
    {
        <!-- Start Post List  -->
        <div class="content-block post-list-view mt--30">
            <div class="post-thumbnail">
                <img src="@_ImageService.ConvertByteArrayToFile(post.ImageData!, post.ImageType!,(int)DefaultImage.BlogPostImage)" alt="Blog Post Images">
            </div>
            <div class="post-content col-8 col-md-5">
                <div class="post-cat">
                    <div class="post-cat-list">
                        <a class="hover-flip-item-wrapper" asp-controller="Categories" asp-action="Details" asp-route-id="@post.CategoryId">
                            <span class="hover-flip-item">
                                <span data-text="@post.Category!.Name">@post.Category.Name</span>
                            </span>
                        </a>
                    </div>
                </div>
                <h4 class="title">
                    <a asp-controller="BlogPosts" asp-action="Details" asp-route-slug="@post.Slug">
                        @post.Title
                    </a>
                </h4>
                <div class="post-meta-wrapper">
                    <div class="post-meta">
                        <div class="content">
                            <h6>
                                @if (post.IsPublished)
                                {
                                    <span class="text-success">Published</span>
                                }
                                else if (!post.IsPublished)
                                {
                                    <span class="text-warning">Unpublished</span>
                                }
                            </h6>
                            <h6 class="post-author-name">
                                <span class="hover-flip-item-wrapper">
                                    <span class="hover-flip-item">
                                        <span data-text="@post.Creator?.FullName">@post.Creator?.FullName</span>
                                    </span>
                                </span>
                            </h6>
                            <ul class="post-meta-list">
                                <li>@post.DateCreated.ToString("MMM dd, yyyy")</li>
                                @if (post.Comments.Count > 1)
                                {
                                    <li><span>@post.Comments.Count</span> total comments on this post.</li>
                                }
                                else if (post.Comments.Count == 1)
                                {
                                    <li><span>@post.Comments.Count</span> comment on this post.</li>
                                }
                                else
                                {
                                    <li>No one has commented on this post yet.</li>
                                }
                            </ul>
                        </div>
                        <form asp-action="Delete">
                            <input type="submit" class="btn btn-lg btn-warning rounded-pill" value=" @if (post.IsDeleted)
                               {
                        <text>Un-Delete</text>
                        }
                        else if (!post.IsDeleted)
                        {
                        <text>Delete</text>
                        }" /> |
                            <a asp-action="Index" class="btn btn-lg btn-primary rounded-pill">Never Mind</a>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    }
</div>
*@